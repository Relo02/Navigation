# Large installers / junk — never commit (this is what blocked the first push)
*.rpm
*.deb

# Upstream FAST-LIO README media (gifs/images) — ~384 MB, not needed to build or
# deploy. Delete these two lines if you want the full upstream docs in the repo.
ros2_ws/src/FAST_LIO_LOCALIZATION_HUMANOID/doc/
ros2_ws/src/FAST_LIO_LOCALIZATION_HUMANOID/FAST_LIO/doc/

# Python / pytest artifacts
__pycache__/
*.py[cod]
.pytest_cache/
